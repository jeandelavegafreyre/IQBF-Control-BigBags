export interface Product { id: string; name: string; isActive: boolean }
export interface UserSummary { id: string; uid: string; firstName: string; lastName: string; fullName: string; role: string | number; isActive: boolean }
